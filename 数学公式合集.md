# 数学公式合集

## 组合数学

### 二项式定理

$$
(a+b)^n = \sum\limits_{i=0}^n \binom{n}{i} a^{n-i} b^i
$$

### 广义二项式系数

$$
\binom{\alpha}{k} = \frac{\alpha (\alpha-1) \cdots (\alpha-k+1)}{k!}, \alpha \in \mathbb{R}, k \in \mathbb{N}
$$

### 广义二项式定理

$$
(a+b)^n = \sum\limits_{i=0}^{\infty} \binom{n}{i} a^{n-i} b^i
$$

### 多项式定理

$$
[x_1^{n_1}x_2^{n_2}\cdots x_m^{n_m}](x_1 + x_2 + \cdots + x_m)^n = \frac{n!}{(n_1!)(n_2!)\cdots(n_m!)}
$$

### 上指标反转

$$
\binom{n}{m} = (-1)^m \binom{m-n-1}{m}
$$

### 斯特林数公式

$$
m^n = \sum\limits_{i=0}^m {n \brace i} m^{\underline{i}}
$$

等式左侧表示 $n$ 个有标号球放入 $m$ 个有标号箱子。

等式右侧表示相同意义，先把 $n$ 个有标号球分成 $i$ 个互相不区分的非空集合，然后把 $i$ 个集合放入 $m$ 个有标号箱子，下降幂表示的就是 $m$ 个里面取 $i$ 个的排列。

通常也可写为：

$$
m^n = \sum\limits_{i=0} {n \brace i} \binom{m}{i} i!
$$

### 第二类斯特林数 行

由上述组合数形式公式，进行二项式反演，可以得：

$$
{n \brace m} = \sum\limits_{i=0}^m \frac{i^n (-1)^{m-i}}{i!(m-i)!}
$$

这是卷积形式，可用 ntt 加速。

### 容斥原理

$$
|\bigcup\limits_{i=1}^n P_i| = \sum\limits_{S \subseteq [1,2,\cdots,n]} (-1)^{|S|-1} |\bigcap\limits_{s \in S} P_s |
$$

### 二项式反演

若：

$$
g_n = \sum\limits_{i=0}^n \binom{n}{i} f_i
$$

则：

$$
f_n = \sum\limits_{i=0}^n \binom{n}{i} (-1)^{n-i} g_i
$$

若：

$$
g_k = \sum\limits_{i=k}^n \binom{i}{k} f_i
$$

则：

$$
f_k = \sum\limits_{i=k}^n \binom{i}{k} (-1)^{i-k} g_i
$$

### 子集反演

若：

$$
F(S) = \sum\limits_{T \subset S} G(T)
$$

则：

$$
G(S) = \sum\limits_{T \subset S} (-1)^{|S|-|T|} F(T)
$$

### 多重组合数

$k$ 种不一样的球，每种球有 $n_i$ 个，所有球总共有 $n$ 个，排列数为 $\binom{n}{n_1,n_2,\cdots,n_k} = \frac{n!}{\prod\limits_{i=1}^k n_i}$

### 二项式推论

#### 范德蒙恒等式

$$
\sum\limits_{i=0}^k \binom{n}{i} \binom{m}{k-i} = \binom{m+n}{k}
$$

可以理解为，将 $m+n$ 个物品分成 $k$ 组，相当于先把 $m+n$ 个物品分成 $n$ 个和 $m$ 个两部分，然后在两组中分别取 $1,k-1$ 个，$2,k-2$ 个等等。

取 $n=k=m$ 可得特殊情况 $\sum\limits_{i=0}^n \binom{n}{i}^2 = \binom{2n}{n}$

#### 朱世杰恒等式

$$
\binom{n}{r}\binom{r}{k} = \binom{n}{k}\binom{n-k}{r-k}
$$

可以理解为，$n$ 个物品选出 $r$ 个优质物品，这 $r$ 个优质物品中再选出 $k$ 个作为最优质物品，等价于 $n$ 个直接选 $k$ 个作为最优质物品，剩下的 $n-k$ 个物品中再选 $r-k$ 个优质物品。

### 卡特兰数

$$
C_0=1,C_n=\sum\limits_{i=0}^{n-1}C_i \times C_{n-1-i}
$$

$$
C_n = \binom{2n}{n} - \binom{2n}{n+1}
$$

## 数论

### 单位函数

$$
\epsilon(n)=[n=1]
$$

单位函数是一个完全积性函数。

#### 莫比乌斯函数

$$
\mu(1)=1
$$

若 $n$ 含有平方数因子：
$$
\mu(n)=0
$$
$n$ 含有 $k$ 个不同质因子：
$$
\mu(n)=(-1)^k
$$
莫比乌斯函数是积性函数。

#### 恒等式

$$
\sum\limits_{d \mid n} \mu(d) = [n=1]
$$



#### 欧拉函数

### 素性测试

##### Fermat 素性测试

由费马小定理，若 $n$ 是素数且 $a \in [1,n-1]$，则 $a^{n-1} \equiv 1 \pmod{n}$，多次随机抽选 $a$ 进行测试。

##### Miller-Rabin 素性测试

二次探测定理：若 $p$ 是奇素数，则 $x^2 \equiv 1 \pmod{p}$ 的解为 $x \equiv 1 \pmod{p}$ 或 $x \equiv p-1 \pmod{p}$

将 $a^{n-1} \equiv 1 \pmod{n}$ 的指数 $n-1$ 分解为 $u \times 2^t$，每次随机的 $a$ 先求出 $v=a^u \bmod n$，然后进行 $t$ 次平方操作，若过程中出现 $v \equiv n-1 \pmod{n}$ 则通过测试。